export interface CrawlResult {
  job_id: string,
  title: string,
  brand: string,
  image_url: URL,
  result_id: string,
}

export interface Job {
  job_id: string,
  job_url: URL,
  job_name: string,
}

